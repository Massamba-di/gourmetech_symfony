{% extends 'base.html.twig' %}

{% block title %}TempsDePreparations{% endblock %}

{% block body %}
    <h1>TempsDePreparations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temps_de_preparation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ temps_de_preparation.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ temps_de_preparation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_temps_de_preparations_index') }}">back to list</a>

    <a href="{{ path('app_temps_de_preparations_edit', {'id': temps_de_preparation.id}) }}">edit</a>

    {{ include('temps_de_preparations/_delete_form.html.twig') }}
{% endblock %}
