<form method="post" action="{{ path('app_temps_de_preparations_delete', {'id': temps_de_preparation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ temps_de_preparation.id) }}">
    <button class="btn">Delete</button>
</form>
