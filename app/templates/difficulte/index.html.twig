{% extends 'base.html.twig' %}

{% block title %}Difficulte index{% endblock %}

{% block body %}
    <h1>Difficulte index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for difficulte in difficultes %}
            <tr>
                <td>{{ difficulte.id }}</td>
                <td>{{ difficulte.name }}</td>
                <td>{{ difficulte.description }}</td>
                <td>
                    <a href="{{ path('app_difficulte_show', {'id': difficulte.id}) }}">show</a>
                    <a href="{{ path('app_difficulte_edit', {'id': difficulte.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_difficulte_new') }}">Create new</a>
{% endblock %}
