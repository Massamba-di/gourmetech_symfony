{% extends 'base.html.twig' %}

{% block title %}Addresses{% endblock %}

{% block body %}
    <h1>Addresses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ address.id }}</td>
            </tr>
            <tr>
                <th>NumeroAddresse</th>
                <td>{{ address.numeroAddresse }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ address.rue }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ address.ville }}</td>
            </tr>
            <tr>
                <th>Postal</th>
                <td>{{ address.postal }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ address.pays }}</td>
            </tr>
            <tr>
                <th>ComplementAddresse</th>
                <td>{{ address.complementAddresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_addresses_index') }}">back to list</a>

    <a href="{{ path('app_addresses_edit', {'id': address.id}) }}">edit</a>

    {{ include('addresses/_delete_form.html.twig') }}
{% endblock %}
